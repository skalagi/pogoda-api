{% extends "base.html.twig" %}

{% block h1 %}
    <span style="float: right; font-size: 21px; color: #df1212">Zanotowana ekstremalnie {{ type }} temperatura!</span>
{% endblock %}

{% if type == 'niska' %}
    {% set time = record.mintime|date("H:i:s") %}
    {% set value = record.min|round(2) %}
{% else %}
    {% set time = record.maxtime|date("H:i:s") %}
    {% set value = record.max|round(2) %}
{% endif %}

{% block big %}
    Dzisiaj o godzinie {{ time }} zanotowano temperaturę {{ value }}℃. <br/>Jest to {{ type == 'niska' ? 'najniższa' : 'najwyższa' }} temperatura w tym {{ period }}.
{% endblock %}


{% block small %}
    {% if value > 25 or value < 5 %}
        Zalecamy ogarniczenie czasu spędzanego na zewnątrz.
    {% endif %}
{% endblock %}
